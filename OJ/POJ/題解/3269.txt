	這題有個很關鍵的地方，就是題目中所提到的"each vertex is an endpoint of exactly one
horizontal edge and one vertical edge"，換句話說，每個直接和橫排的點數，都會是偶數，而
且連接的順序剛好會是第一個點和第二個點，第三個點和第四個點...以此類推。用這個想法，就
可以很輕易算出我們要的答案，不過前提是得先確認有沒有「洞」形成，這點可以在計算的過程中
，建立一個鄰接表，之後用該表遍歷每個點確認。