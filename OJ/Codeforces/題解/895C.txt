仔細觀察，ai最大才70。而一個數為另外一個數的平
方的條件是：他的質因數分解中，每個質因數的次方
必須皆為偶數，而小於70的質數才19個，我們又只關
心次方是不是偶數，因此將數字的質因數分解用零和
一來表達：0代表有偶數個：1代表有奇數個。但這樣
dp的狀態數依然高達10^5 * 2^19。必須再減少。
同一個數自乘，只會有兩種結果。因此可以先將相同
的數字合併起來，分別計算兩種結果的方法數。而數
字只會有70種，這樣一來狀態數就降低到70 * 2^19
了。