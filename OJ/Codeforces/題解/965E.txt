初始想法是這樣：先從前綴長度為1開始，字串相同
的分一組，在每一組裡，選出一個維持不變，然後
把剩下字串加長，然後再把相同的分組,...就這樣
直到有n組。可以發現，這個過程其實就是在走訪trie
樹。因此我們直接在trie上思考。
把字串轉化為trie後，問題就轉化成：將字串的結
尾往樹根移，同時不能有兩個字串的結尾在同一個
節點上，並使所有字串結尾的深度總和最小。考慮
某個子樹，為了使深度總和最小，就得把這棵子樹
上，深度最深的那個字串結尾，拉到這棵子樹的樹
根上(如果樹根沒有其它的字串結尾的話)，就這樣
從底部一路往上計算，直到根節點。