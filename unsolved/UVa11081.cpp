#include<iostream>
#include<cstring>
#define MAX 60
using namespace std;

int dp[MAX+1][MAX+1][MAX+1];

int main(){
	string str1, str2, result;
	cin>>str1>>str2>>result;
	memset(dp, 0, sizeof(dp));
	return 0;
}